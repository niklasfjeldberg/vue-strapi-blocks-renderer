{
  "name": "vue-strapi-blocks-renderer",
  "private": false,
  "version": "0.0.8",
  "description": "A Vue renderer for the Strapi's Blocks rich text editor.",
  "repository": "https://github.com/niklasfjeldberg/vue-strapi-blocks-renderer",
  "license": "MIT",
  "type": "module",
  "main": "./src/composables/index.ts",
  "files": [
    "./src/composables"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "vitest run",
    "release": "changelogen --release && npm publish && git push --follow-tags",
    "build2": "vue-tsc && vite build",
    "release2": "npm run lint && npm run test && changelogen --release && npm publish && git push --follow-tags"
  },
  "peerDependencies": {
    "vue": "latest"
  },
  "dependencies": {},
  "devDependencies": {
    "vue": "latest",
    "@vitejs/plugin-vue": "^4.5.2",
    "changelogen": "^0.5.5",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vitest": "^1.1.0",
    "vue-tsc": "^1.8.25"
  },
  "keywords": [
    "strapi",
    "blocks",
    "renderer",
    "vue"
  ]
}
